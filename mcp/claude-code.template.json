{
  "_comment": [
    "Auto-generated to ~/.claude/mcp.json by SessionStart.sh on every session.",
    "Env var substitution: ${SAMVERK_HOST} (default: 192.168.1.162), ${SAMVERK_AUTH_TOKEN}.",
    "No secrets in this file -- tokens are resolved from environment at runtime.",
    "Corresponding remote Claude.ai connectors are denied in settings.template.json",
    "to prevent OAuth redirect conflicts in autonomous sessions."
  ],
  "mcpServers": {
    "synapset": {
      "type": "http",
      "url": "http://${SAMVERK_HOST}:6464/mcp",
      "headers": {
        "Authorization": "Bearer ${SAMVERK_AUTH_TOKEN}"
      }
    },
    "samverk": {
      "type": "http",
      "url": "http://${SAMVERK_HOST}:8080/mcp",
      "headers": {
        "Authorization": "Bearer ${SAMVERK_AUTH_TOKEN}"
      }
    }
  }
}
